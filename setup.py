from setuptools import setup

# This file is only for development installation
# The actual build configuration is in pyproject.toml

setup(
    name="funannotate2",
    version="24.11.02",
    packages=["funannotate2"],
    include_package_data=True,
)
